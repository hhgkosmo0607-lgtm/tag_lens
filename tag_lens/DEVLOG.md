# TAG_LENS 개발일지

세션별 작업 로그. 프로젝트 개요/구조는 [README.md](README.md) 참고.

## 2026-07-09

### 완료

- **주간/야간 카메라 시계 오류 감지 구현**: DEVLOG 2026-07-08의 "OffsetTimeOriginal vs OffsetTime 불일치" 아이디어를 실제 데이터로 먼저 검증
  - **원안(단순 불일치 검출)은 기각**: `uploads/` 실사진 26장 중 25장이 오프셋 태그를 갖고 있었는데, 그중 **21장(84%)**이 `OffsetTimeOriginal != OffsetTime`이었음 — "드문 카메라 시계 오류"가 아니라 이 데이터셋에서는 대다수 케이스였음. 이 기준 그대로 썼다면 EXIF 우선 판정 자체가 무력화될 뻔함
  - **패턴을 좁혀서 재설계**: 21건 중 12건은 `OffsetTimeOriginal == "+00:00"`(카메라/파이프라인이 시간대를 몰라 쓰는 흔한 기본값)이면서 `OffsetTime`은 실제 시간대(+08:00 등)를 가리키는 케이스였음. 이 12건을 실제 사진으로 육안 검증(`93eeb38b`: DTO 00:39인데 실제론 쨍한 대낮 건물 사진, `62b3d6e6`: DTO 04:00인데 실제론 대낮 요트 사진, `b6b7ad7e`: DTO 22:16인데 실제론 대낮 승마 사진) — 전부 `OffsetTime` 오프셋(+08:00)만큼 보정하면 실제 촬영 상황과 정확히 일치함을 확인
  - 나머지 9건(`+08:00` vs `+09:00`처럼 둘 다 0이 아닌 값끼리 다른 경우)은 여행/편집으로 인한 정상적인 시간대 차이로 보고 보정 대상에서 제외 — DateTimeOriginal 자체가 이미 촬영지 로컬 시각이라 보정하면 오히려 틀어짐. 실제로 이 9건은 보정 여부와 무관하게 주야간 경계에서 멀어서 결과에 영향도 없었음
  - `ai/daynight_predict.py`에 `get_exif_datetime_and_offsets()`/`extract_exif_hour()` 추가 — `OffsetTimeOriginal == "+00:00"`이고 `OffsetTime`이 다른 값일 때만 `OffsetTime` 오프셋만큼 시각을 보정
  - **EXIF 파싱 로직 중복 제거**: `app.py`와 `scripts/maintenance/reclassify_photos.py`에 각각 따로 있던 `_get_exif_datetime_str()`/`extract_exif_hour()` 구현을 `ai/daynight_predict.py` 하나로 합침(다른 판단 로직들처럼 `ai/`에 모으는 기존 원칙과 일치). `app.py`의 `extract_photo_date()`도 같은 공유 함수를 씀
  - `scripts/maintenance/reclassify_photos.py`로 기존 26장 재분류해서 실측 검증: `93eeb38b`/`62b3d6e6`/`b6b7ad7e` 전부 `#주간`으로 정정됨 확인. 장르 변경은 0건(주야간만 영향)
- **주간/야간 경계 안전장치 추가**: 실서비스 테스트 중 사용자가 업로드한 실사진(`04541da4`, 명백한 야경/수면 반사 사진)이 `#주간`으로 오분류된 것을 발견해 원인 추적
  - 원인: EXIF 원본 시각이 `06:17:29`로 주간 경계(06시)에 바로 걸쳐있었음 — 위에서 만든 `+00:00` 보정을 거쳐도(→14:17) 여전히 틀렸음. 시간대 문제가 아니라 카메라 시계 자체(원본 06:17 값)가 신뢰할 수 없었던 사례로, 오늘 만든 보정 로직으로는 못 잡는 새로운 실패 유형
  - CNN 단독 예측을 확인해보니 87.6% 확신도로 정확히 "야간"이라고 판정 — 경계 근처 EXIF는 CNN 교차검증으로 잡을 수 있음을 실측으로 확인
  - `ai/daynight_predict.py`의 `resolve_daynight()`에 안전장치 추가: EXIF 시각이 (보정 전/후 둘 중 하나라도) 주간/야간 경계 06시·18시에서 `DAYNIGHT_BOUNDARY_MARGIN=1`시간 이내면 CNN으로 교차검증해서, CNN이 70% 이상 확신도로 다른 판정을 내리면 CNN을 따름. 경계에서 먼 EXIF 시각은 여전히 그대로 확정 판정(그림자 짙은 대낮 숲 사진에서 CNN이 정확한 EXIF를 뒤집었던 예전 3중 다수결의 역효과가 재발하지 않도록 범위를 좁힘)
  - 처음엔 보정 *후* 시각만으로 경계를 체크했다가 놓칠 뻔함(06시→14시로 보정되면서 경계에서 멀어져 버림) — `extract_exif_hour()`가 `(보정된 시각, 원본 시각)` 튜플을 반환하도록 바꿔서 원본 시각도 같이 확인하도록 수정. `resolve_daynight()`에 `raw_exif_hour` 파라미터 추가, `app.py`/`reclassify_photos.py` 호출부도 함께 수정
  - `04541da4` 재검증: `#주간` → `#야간`으로 정정 확인
- **실내/실외 판별에서 `자연` 장르 제외**: 위 실사진 82장(실제 사용자 업로드분)으로 재분류해서 태그 분포를 살펴보다가 `자연` 장르 사진의 `#실내` 오탐이 도드라짐을 발견(주간 33%, 야간 56%가 `#실내`로 오탐)
  - 육안 검증 6건 중 5건이 오분류: 야외 야경 사진 2건(밤바다 `04541da4`, 등불 축제 `d50d744b`) + 기존에 알려진 벽/간판 클로즈업 1건(ZARA 매장 사인 `f2387c1c` — 이 사진은 장르 자체도 `자연`으로 잘못 붙어있었음) + 장노출/카메라 흔들림 추상 사진 2건(`c63bf3c3`, `6c20de40` — 장르 CNN이 애초에 피사체를 못 알아봐서 `자연`으로 잘못 분류된 케이스, 실내/실외 자체가 답이 없는 입력). 나머지 1건(`db9fb254`, 창문/난간 너머로 찍은 바다)만 실제로 실내에서 촬영해 정탐일 가능성
  - `자연` 장르는 정의상 거의 항상 실외라 진짜 실내인 경우가 드문 반면, 이런 오탐이 훨씬 흔했음 — 재학습 없이 바로 적용 가능한 개선으로 `INDOOR_CHECK_GENRES`에서 `자연` 제외(`도시`/`인물`만 유지, 스튜디오/카페처럼 실내외 구분이 실제로 의미 있는 장르라 남겨둠)
  - `INDOOR_CHECK_GENRES`가 `app.py`/`reclassify_photos.py`에 중복 정의돼 있던 것도 `ai/indoor_predict.py`로 합침(오늘 EXIF 때와 같은 패턴)
  - 재분류 82장으로 재검증: 위 6건 중 5건(자연 장르는 전부) `#실내` 태그 사라짐 확인, `도시`/`인물` 장르의 `#실내` 판별은 그대로 유지됨
- **개발 서버 실사용 테스트**: Flask 개발 서버를 띄우고 브라우저로 실제 사진 82장을 업로드해보는 방식으로 위 두 버그를 실측 발견함 — 지금까지의 스크립트 기반 검증만으로는 못 잡았을 케이스들
- **얼굴 검출 신규 오탐 발견 및 수정**: 카페 벽에 붙은 사진 콜라주(`7bb1a9cc`)가 `#인물`로 오분류된 것을 확인 — `count_faces()`가 얼굴을 1개 감지했는데, 실제로는 콜라주 속 자판기 사진의 버튼 패널(원형+대칭 격자 무늬)이었음(bounding box 시각화로 확인). 신뢰도 0.642로, 기존 임계값(0.6)을 살짝 넘겨 통과한 경계 사례
  - 82장 전체의 얼굴 검출 신뢰도 분포를 뽑아보니, 이 오탐(0.642) 바로 위 진짜 얼굴이 0.656(인물 사진 육안 확인)이라 둘 사이 간격이 0.014뿐 — `ai/face_detect.py`의 `min_detection_confidence`를 0.6 → **0.65**로 올려서 그 사이를 가름
  - 재분류 82장 재검증: 이 사진만 `인물 → 기타`로 정정(`#실내 #인물` 태그 사라짐), 나머지 81장은 변경 없음(진짜 얼굴 검출에 회귀 없음, 가장 낮은 진짜 얼굴 신뢰도도 0.656이라 새 임계값 통과)
- **실내/실외 `도시`/`인물` 장르 재학습 착수** (사용자 요청 — "학습을 해"): `자연` 제외 후에도 `도시`/`인물`의 `#실내` 오탐 14건 중 9건이 확신도 90%+로 틀린 것을 실측 확인(임계값 조정으로는 해결 불가 — 오탐도 정탐과 같은 확신도 범위). 원인은 "인공 구조물이 화면을 채우고 하늘이 안 보이는 실외"(건물 벽/간판, 반사 유리, 다리, 야시장) 패턴이 광범위함
  - 새 데이터셋 다운로드 없이 기존 로컬 자산만 재활용(`scripts/expand/expand_indoor_no_sky.py`): (1) `dataset_daynight/야간`(DNIM 실외 웹캠 774장)을 실외로 재활용 — "야간=실내"라는 잘못된 상관관계 차단, (2) 기존 실외 사진 1000장의 상단 30~50%를 무작위로 잘라내 "하늘 안 보이는 실외" 변형 합성 — "하늘 보임=실외"라는 얕은 지름길 차단
  - 실외가 실내보다 훨씬 많아져서(train 4996 vs 2800) `train_indoor_model.py`에 `class_weight` 추가해 손실 함수에서 균형을 맞춤
  - 재학습 진행 중(백그라운드) — 완료되면 오늘 찾은 오탐 9건으로 재검증 예정
- **동물 판별 로직을 top-1 단일 인덱스 → 카테고리별 확률 합산 방식으로 변경**: 실사용 테스트에서 동물 태그 오탐 발견(고양이 사진에 `#강아지`, 장미 사진에 `#동물`) — 원인 조사 중 별개로, 의자 위 고양이 사진(`57ceb773`)이 태그가 아예 안 붙은 걸 발견. top-1이 "Egyptian_cat" 25.5%로 임계값(0.3) 미달이었는데, ImageNet은 개 품종이 118개나 되는 반면 고양이 품종은 5개뿐이라 고양이 쪽 확률이 여러 품종으로 잘게 쪼개져 저평가됨(품종 5개 합산 시 37.0%로 임계값 통과)
  - `ai/animal_predict.py`의 `predict_animal()`을 강아지/고양이/기타동물/비동물 4개 버킷으로 확률을 합산한 뒤 최댓값을 취하도록 변경(비동물 버킷보다 낮으면 태그 없음)
  - 82장 전체 재검증: 이전에 놓쳤던 고양이 사진 2장(`8078c82e`→`#고양이`, `51a90bd0`→`#동물`)을 새로 잡아냄, 기존 정탐 2건 회귀 없음
  - 단, 이 방식으로도 못 잡는 근본적 오분류 2건 확인(재학습 없이는 못 고침, 한계로 남김): 특이 자세 고양이가 "강아지" 버킷 76%로 뭉텅이 오분류, 장미꽃이 "기타동물"(조류) 버킷 72%로 오분류 — 둘 다 확률 분산 문제가 아니라 모델 자체가 깊이 잘못 판단한 경우라 카테고리 합산으로도 해결 안 됨
- **82장 전수 조사로 신규 문제 2건 추가 발견**(재학습 필요, 오늘 미해결)
  - **daynight EXIF 새 실패 유형**: 장미 사진(`5b9c3f20`)이 명백한 대낮인데 EXIF가 23시로 기록됨 — 오늘 만든 `+00:00` 보정 패턴이 아닌 **다른 종류의 카메라 시계 오류**라 못 잡음. 게다가 CNN도 68% 확신도로 야간 오판(그림자 짙은 나무 — 기존에 알려진 "숲 사진 47% 오분류"와 같은 계열)해서 EXIF+CNN 두 신호가 우연히 같은 방향으로 틀려 경계 안전장치도 무력화됨
  - **장르 CNN의 동물→음식 오분류 신규 변종**: 의자 위 고양이(`57ceb773`)가 "음식"(62%)으로 분류됨. 어제 고친 "동물→인물" 보정(`resolve_genre_with_animal`)은 top-1이 인물일 때만 작동해서 이 케이스엔 안 걸림 — 동물 판별 로직 개선으로도 이 사진은 여전히 동물 태그가 안 붙어서(비동물 버킷이 더 높음) 보정 자체가 트리거 안 됨
- **나머지 태그(`#흑백`/`#야경`/`#따뜻한`·`#차가운`) 전수 감사**: 사용자 요청으로 82장 전체를 태그 카테고리별로 훑음
  - `#흑백` 17건 전부 육안 검증 정확, `#야경` 6건 전부 조건(자연·도시+야간) 정상 작동(장미 사진 1건은 위 daynight EXIF 버그의 파생 오류일 뿐 별개 버그 아님)
  - `is_bw`/`color_tone` DB 컬럼과 실제 태그 간 모순 전수 검사 — 0건, 정합성 문제 없음
  - **색온도(`#따뜻한`/`#차가운`) 로직 점검**: `opencv/feature_analyzer.py`가 R채널 평균과 B채널 평균만 비교하고(G채널 미반영) 판단하는 걸 발견 — 청록색(G+B 높음) 피사체 사진(`bb322a9c`)이 전체적으로 살짝 따뜻한 조명이라 `#따뜻한`으로 붙는 사례 확인. 버그가 아니라 "카메라 화이트밸런스 warmth"(설계 의도)와 "피사체 고유 색상"이 다른 질문이라 생기는 설계상 한계 — 사용자에게 확인 후 **현재 방식(조명/화이트밸런스 기준) 유지로 결정**, 코드 변경 없음
- **실내/실외 재학습 시도 → 역효과로 롤백 → CLIP 제로샷으로 교체** (사용자 요청 — "학습을 해"): `expand_indoor_no_sky.py`로 데이터 보강(DNIM 야간 웹캠 774장 + 기존 실외 사진 크롭 1000장) 후 재학습. 학습 중 Flask 서버와 체크포인트 파일 접근이 겹쳐 Windows 파일 잠금 충돌로 2단계 4에폭째에 죽는 사고 발생(모델 파일 자체는 안 깨짐) — `train_indoor_model.py`가 임시 파일에 저장 후 완료 시에만 서비스 파일 교체하도록 고치고, 죽은 지점(2단계 3에폭)부터 이어서 재개하는 `scripts/train/resume_indoor_training.py` 작성해 1단계(~45분)를 다시 안 돌리고 완료
  - 검증셋 정확도 97.49%로 좋아 보였으나, 오늘 찾은 실사진 15장 재검증에서 **13.3%**(거의 전부 "실내"로 오판, 확신도 90~100%)로 오히려 더 나빠짐 — 보강 데이터의 좁고 특정한 시각적 패턴(웹캠 그레인, 크롭 아티팩트)에 과적합된 것으로 추정. 검증셋 점수만 보고 바로 서비스 파일에 반영한 게 문제였음(교훈: 재학습 후 실제 사진 테스트를 반드시 통과해야 서비스 파일로 승격시켜야 함) — `git checkout`으로 롤백
  - 롤백한 모델도 같은 15장에서 오탐률 100%(14/14)로 확인 — 이 15장은 "이미 알려진 실패 패턴"에 맞춰 고른 사진들이라 전반적 정확도는 아니지만, 적어도 이 패턴에서는 재학습 전부터 심각했음
  - CLIP 제로샷("a photo taken indoors" vs "outdoors")을 같은 15장으로 테스트 → 정확도 73.3%, 오탐률 28.6% — `ai/indoor_predict.py`를 CLIP으로 교체. 실사진 재분류(82장)로 재검증: 도시/인물 25장 중 `#실내` 태그 14건 → 3건(그중 2건은 육안 확인 결과 정탐 — 피아노 연주, 돔 천장 서점), 실질 오탐은 HAKATA 간판 1건만 남음
- **daynight/동물 판별도 CLIP으로 교체**: 사용자 요청으로 `자연` 장르 daynight 오분류와 `#고양이` 오분류 원인을 조사하다가, 둘 다 CLIP으로 해결됨을 확인해 같은 패턴 적용
  - **daynight**: `자연` 장르 21장 중 EXIF-CNN 불일치 7건 발견(2건은 안전장치가 이미 정상 처리한 케이스, 순수 CNN 오분류는 5건) — 숲 캐노피(하늘 안 보임)뿐 아니라 특이 색보정(보라빛 하늘) 사진도 포함돼 있어 "숲 그늘"만의 문제가 아니라 CNN 학습 데이터(CCTV 도로 400장 + DNIM 도시 웹캠) 자체가 자연광/자연 사진 도메인을 아예 학습한 적 없다는 게 근본 원인으로 재확인됨. 같은 5장으로 CLIP 테스트 → 4/5 정확(CNN은 5/5 전부 오답) → `ai/daynight_predict.py`의 `predict_daynight()`(EXIF 없을 때 폴백 + 경계 안전장치 교차검증에 쓰임)를 CLIP으로 교체
    - **알려진 회귀 1건**: `04541da4`(밤바다)가 오늘 오전에 이미 고쳤던 케이스인데, CLIP이 이 특정 사진에서 65.1%로 틀리게 "주간"이라고 판단해서 EXIF(오답)와 CLIP(오답)이 같은 방향으로 일치해버려 경계 안전장치가 뒤집을 대상이 없어짐(임계값 문제가 아니라 CLIP 자체 오답 — 예전 CNN은 이 사진에서 87.6%로 정답이었음). CLIP이 전반적으론 훨씬 낫지만 이 개별 사진 하나는 역행함 — 재학습 없이는 못 고치는 잔여 이슈로 남김
  - **동물**: ImageNet 기반 판별의 두 오탐(고양이→강아지 76%, 장미→동물 41%) 원인은 ImageNet이 "종"이 아니라 "품종"(개 118개 vs 고양이 5개) 분류용이라 생기는 근본적 편향. CLIP 제로샷 테스트 → 최초 4/4 정확했으나, `ai/animal_predict.py`에 실제 반영해서 82장 전체로 재분류해보니 새로운 오탐 2건 발견(인물 사진에 "야생동물/새" 60%, 커피 사진에 "말" 36%) — 조사해보니 CLIP이 부정문 프롬프트를 잘 못 다루고(음성 걸러내기용 "동물이지만 개/고양이/말 아님" 프롬프트가 장미에도 41%나 걸림), softmax 상대비교만으로는 "둘 다 약하게 안 맞지만 그나마 동물 쪽이 근소 우세"인 경우까지 동물로 오판(원본 유사도 로짓이 정탐 24~28점대 vs 오탐 21점 이하로 뚜렷이 갈림)하는 걸 확인 → 프롬프트를 구체적 긍정문으로 전부 교체 + 상대비교와 절대 로짓 임계값(23.0)을 모두 통과해야 동물로 판정하는 이중 게이트 추가. 8개 케이스(고양이 3, 장미, 승마, 새, 인물, 커피)로 전부 검증 완료
  - **CLIP 로더 통합**: `indoor_predict.py`/`daynight_predict.py`/`animal_predict.py`가 각자 CLIP을 따로 로드하던 걸 `ai/clip_utils.py`(공유 `get_clip()`/`image_to_pil()`)로 통합 — 메모리에 동일 모델 3중 로드하던 것 정리
  - `requirements.txt`에 `torch`/`transformers` 추가
- **장르 CNN도 CLIP 제로샷으로 교체**: 아스팔트 타이어자국 클로즈업이 `#자연`(98.6% 확신도로 오답)으로 태그되는 걸 발견해 조사 — indoor/daynight/animal과 같은 "맥락 정보 부족한 클로즈업/텍스처" 실패 패턴임을 확인
  - 교체 전 알려진 5개 실패 사례로 먼저 검증: 아스팔트(자연 98.6%→도시 65.0%), TSINGTAO 건물 파사드(사물 58%, "도시"가 후보에도 없었음→도시 80.8%), ZARA 매장 간판(자연 94%→사물 61.3%), 장노출 추상사진(→확신도 낮아 `기타`로 정착), 흔들림 사물 사진(사물 89.8% — CLIP도 확신도 높게 오답, 재학습 없이는 못 고치는 한계로 사용자 확인 후 수용)
  - `ai/genre_predict.py`의 `predict_genre()`를 CLIP 제로샷으로 교체(`LOW_CONFIDENCE_THRESHOLD=0.6` 패턴은 그대로 유지), 82장 전체 재분류 → **35장(42.7%) 변경**, 예상보다 훨씬 큰 변화라 스팟 체크로 원인 분석
    - 세부 내역: `#기타`로 떨어진 것 15장(5-way 단일 프롬프트라 확신도 60% 못 넘는 경우가 많아짐 — 정보 손실 위험), `#기타`에서 구체적 장르로 나온 것 9장(개선), 두 구체적 장르끼리 바뀐 것 10장(대부분 개선)
    - 육안 검증: 플라스틱 의자 사진(`#음식`→`#사물`, CNN이 원래도 틀렸었음, 개선), "CEREAL" 매거진(`#음식`→`#기타`, 실제론 음식이 아니라 잡지라 오히려 더 정직해짐), 뱅크시 벽화 흑백사진(`#도시`→`#기타`, 애매해서 판단 보류), 해변 풍선 든 사람 사진(`#자연`→`#기타`, 원래 자연이 그럭저럭 맞는 태그였는데 정보 손실된 사례로 보임)
    - 사용자에게 결과 요약 후 임계값/프롬프트 추가 튜닝 여부를 물었으나, "직접 하나하나 보겠다"며 브라우저로 직접 확인하는 쪽 선택 — 추가 코드 변경 없이 현재 상태 유지
- **동물 오탐 3건 발견 및 근본 원인 기반 수정**: 사용자가 `9a088720`(완전히 깜깜한 야경 수면반사 사진, 작은 실루엣 하나)에 `#동물`이 붙은 이유를 물어서 조사하다가 발견
  - CLIP의 근본 구조 설명: 바운딩박스 없이 사진 전체와 프롬프트 문장의 전역 유사도만 비교하고, 프롬프트 중 반드시 하나를 고르는 forced-choice 구조라 진짜 동물이 없어도 색감/구도가 막연히 "동물틱"하면 뭔가는 1등으로 뽑힘
  - `predict_animal()`의 기존 이중 게이트(상대 비교 + 절대 로짓 23.0)로도 못 거르는 오탐 3건 확인: `9a088720`(어두운 반사, 새 로짓 24.99 — 기존 정탐 구간 24~28 안에 들어감), `c63bf3c3`(빛번짐 장노출, 새 로짓 23.32), `c7d60746`(구름 낀 해안 풍경, 새 로짓 23.08) — 절대 임계값을 더 올리는 걸로는 정탐과 안 갈림
  - 새 신호 발굴: **종(species) 프롬프트 1등-2등 로짓 차이(gap)**. 진짜 동물 사진은 정답 종만 확 튀고 나머지는 한참 낮은데(정탐 8건 실측: 말 5.25, 새 4.23, 고양이 6건 3.68~5.68), 위 오탐 3건은 개/고양이/말/새가 다 고만고만하게 붙어있었음(gap 0.80~1.56) — 깔끔하게 분리됨
  - `_ANIMAL_SPECIES_GAP_THRESHOLD = 2.5` 추가, 정탐 8건(고양이 6, 말 1, 새 1) + 오탐 3건 총 11건으로 재검증 → 11/11 정확. 82장 재분류 → **1장만 변경**(`c7d60746`: 동물 오탐 제거로 `resolve_genre_with_animal` 강제 보정이 안 걸려서 `#기타`→`#자연`으로 원래 장르 회복) — 의도한 것만 정확히 고쳐짐, 회귀 없음
- **README.md를 오늘 변경사항(CNN→CLIP 전환)에 맞춰 갱신**: 주요 기능/프로젝트 구조(`ai/clip_utils.py` 추가, indoor는 별도 로더 쓰는 것 명시)/파이프라인 순서(실제 `app.py` 순서와 일치하도록 재정렬: 장르→동물→`resolve_genre_with_animal`→주야간→실내외)/모델 섹션(CLIP 현재 사용 vs CNN 레거시로 구분)/데이터셋 출처 표(레거시 CNN 학습용임을 명시) 전체 반영
- **얼굴 검출 신규 오탐 발견, short-range 전환 실측 검증 후 기각**: `bd6413f7`(사람 없는 거리 사진, 벽에 붙은 화재경보기 벨)가 `#인물`로 오탐됨을 발견 — 좌표까지 확인해보니 검출 박스(487,544,57×57)가 정확히 그 벨 위치와 일치, 신뢰도 0.696으로 오늘 올린 임계값(0.65)을 넘겨버림. mediapipe full-range 모델이 재현율을 높이려 원형/대칭 패턴을 얼굴로 착각하는 특성(자판기 버튼 패널 오탐과 같은 계열) 때문
  - "short-range 모델로 바꾸면 어떻게 되나" 질문에 실측으로 답함: `blaze_face_short_range.tflite` 받아서 현재 얼굴 검출로 `#인물`이 붙은 12장 + 오탐 2장(`bd6413f7`, `7bb1a9cc`)에 직접 돌려 비교 — 오탐 2건은 둘 다 안 잡음(개선)이나, **진짜 얼굴 12장 중 9장을 놓침**(승마 사진 `f304137a`, 해변 인물 사진 `a05ae4c1` 등 포함, 3장만 검출: 0.816/0.922/0.562). short-range는 "2m 이내 정면 위주"라 카메라에서 떨어졌거나 옆모습인 얼굴을 대량으로 놓침 — 오탐 1건 고치려다 진짜 인물사진 9건을 잃는 손해가 훨씬 커서 **전환 기각**, 코드 변경 없이 알려진 한계로 남김(다운로드했던 short-range 모델 파일/테스트 스크립트는 삭제)
- **장르 CLIP이 역광 군중 사진을 "음식"으로 오분류하는 것 추가 발견 및 수정**: 야시장에서 줄 서있는 사람들(`a20242bb`, 흑백, 얼굴 미검출)이 "음식" 82.1%로 오분류됨을 발견. 군중/부스 부분만 잘라서 테스트해보니 각 부분은 오히려 "인물"이 1등이었음(전체 구도가 조합될 때만 노점상 사진 스타일과 통계적으로 겹치는 것으로 추정) — `_GENRE_PROMPTS`에 "군중" 프롬프트를 추가해 "인물" 프롬프트와 합산하는 방식으로 해결(day/night·indoor와 같은 다중 프롬프트 합산 패턴). 회귀 테스트 14건 통과
  - 82장 재분류 중 신규 부작용 발견: 같은 얼굴 그림(모나리자)이 반복 인쇄된 포스터 매대 사진(`820845b5`)이 "군중" 프롬프트에 낚여 기타→인물로 오분류됨 — "인쇄물/포스터가 반복 진열된 사진" 거부권 프롬프트를 추가해서, 이 로짓이 인물/군중 로짓보다 높으면 군중 보너스만 취소하도록 수정(순수 "person" 프롬프트 점수는 유지). 82장 재분류 → 이 1장만 정확히 되돌아감(기타), 다른 케이스 영향 없음
- **얼굴 검출 오탐(화재경보기)을 CLIP 2차 검증으로 최종 해결**: short-range 전환은 기각했지만 문제 자체는 남아있어서 다른 방법 모색 — mediapipe 얼굴 특징점(눈/코/입 좌표) 기하학 비율을 시도했으나 오탐과 진짜 얼굴이 겹쳐서 실패. 대신 검출된 박스를 60% 여백 두고 잘라 CLIP에게 "사람 얼굴 클로즈업" vs "얼굴 아닌 원형 물체" 제로샷으로 재확인하는 방식 시도 → 실측(진짜 얼굴 10건: 0.270~0.958, 오탐 1건: 0.049)에서 깨끗하게 갈림, `_FACE_CROP_VERIFY_THRESHOLD=0.15`로 설정. `ai/face_detect.py`의 `detect_faces()`에 `_verify_face_crop()` 검증 단계 추가(CLIP 실패 시엔 안전하게 통과시킴)
  - 부수 효과 발견: 야경 다리 사진(`f93a88a9`)처럼 화면 대비 너무 작아 크롭해도 얼굴처럼 안 보이는 "배경 속 행인" 얼굴(신뢰도 0.031)도 같이 걸러짐 — 사진 주제가 도시/야경인데 화면의 0.19%밖에 안 되는 얼굴 하나 때문에 장르가 통째로 "인물"로 강제되던 문제(별도로 얼굴 크기 비율만으로 걸러보려 했으나 진짜 소형 인물사진 2건과 겹쳐 실패했던 문제)까지 부수적으로 해결됨
  - 82장 재분류 → 정확히 2건만 변경(`f93a88a9`, `bd6413f7` 둘 다 인물→도시), 나머지 80장 회귀 없음
- **동물 태그에 화면 대비 크기 게이트 추가**: 노을진 바다 사진(`3b6cb7d5`) 속 점만 한 새(전체 화면의 0.02% 미만)까지 `#동물`이 붙는 게 과하다는 피드백 — CLIP은 전역 유사도만 재서 크기 정보가 없어 "곁다리 동물"을 못 거름. 사진을 축소해서 확신도 붕괴 여부로 판단하는 방법을 먼저 시도했으나 실패(축소 강건성이 실제 크기와 무관 — 확실한 승마 사진이 32px 축소에서 오히려 확신도 폭락, 점만 한 새는 16px까지 축소해도 86%대 유지)
  - 대신 mediapipe **Object Detector**(EfficientDet-Lite0, COCO 90-class 사전학습)를 새로 받아 실제 바운딩박스로 크기를 직접 측정하는 방식으로 해결 — 이미 쓰던 mediapipe 라이브러리의 다른 Task라 새 pip 패키지 설치는 불필요, 모델 파일(`models/efficientdet_lite0.tflite`, 4.6MB)만 추가. 낮은 임계값(0.02)으로 최대 재현율 감지 후 COCO 동물 카테고리 중 최대 박스 면적 비율 계산
  - 실측: 점만 한 새는 최선의 감지조차 0.047%인 반면, 확실한 동물 사진 7건(고양이 6장 4.4~71.2%, 말 1장은 이 모델이 "말"로는 잘 못 잡지만 그 자리 박스 면적 7.7%)은 전부 1% 이상 — 165배 격차라 `_MIN_ANIMAL_AREA_RATIO=0.01`(1%)로 설정. `predict_animal()`의 마지막 관문으로 추가(감지기 실패 시 안전하게 통과)
  - 82장 재분류 → `3b6cb7d5` 1건만 변경(`#동물` 제거), 부수 효과로 `resolve_genre_with_animal` 강제 보정도 안 걸리게 돼서 장르가 `#기타`→`#자연`으로 자동 회복. 나머지 81장 회귀 없음
- **실내/실외 판별을 모든 장르에 적용하도록 변경**: `#실내` 태그가 어느 순간부터 거의 안 보인다는 피드백 — 원인은 `INDOOR_CHECK_GENRES = {"도시","인물"}` 화이트리스트 때문에, 오늘 장르 CLIP 변경으로 사진들이 `기타`/`사물` 등으로 많이 빠지면서 실내 판별 대상에서 자동 제외된 것. 사물/음식/기타 샘플로 재검증해보니 CLIP은 합리적으로 판단했고(카페 벽 콜라주 실내 99.5%, 뱅크시 벽화 실외 94.9% 등), 자연 장르 9장도 전부 정확히 실외(87.7~100%) — CNN 시절 자연 오탐 패턴이 CLIP엔 없어서 화이트리스트 자체를 제거. `app.py`/`reclassify_photos.py`의 genre 조건분기 삭제, `INDOOR_CHECK_GENRES` export도 제거. 82장 재분류 → `#실내` 태그 2장→26장
- **실내/실외 프롬프트 개정 (야간 매장 전면 오탐 수정)**: 위 확대 적용 후 `ad97e91d`(가게 앞에 세워둔 자전거, 밤이라 하늘 안 보이고 조명 켜진 매장 전면이 배경을 채움)가 실내 89.2%로 오판된 걸 발견 — CNN 시절부터 있던 "밤+하늘 안 보임+조명" 실패 패턴이 CLIP에도 약하게 남아있었음. "카메라가 실내에 있다" vs "실내스러운 디테일이 화면에 보인다"를 구분 못 하는 게 원인으로 보여, 프롬프트를 촬영자 위치를 명시하는 문장으로 교체(`"the camera is indoors/outdoors..."`) — 니이브하게 야외 프롬프트만 추가하는 약한 시도(84.1%까지만 하락)는 부족해서 기각하고, 이 강한 버전으로 교체. 실측: 타겟 89.2%→58.2%(임계값 미만으로 태그 제거), 기존 정탐 8건(실내 4+실외 4) + 자연 9건 전부 회귀 없음. 82장 재분류 → `#실내` 26장→22장(추가 오탐 정리)
  - `a20242bb`(야시장 부스 사진)는 여전히 실내로 남음 — 천막/지붕 아래일 가능성 있어 오탐인지 불확실, 미해결로 둠
- **태그 UI 개선 2건**: (1) 갤러리 필터의 태그를 체크박스+"필터 적용" 버튼 방식에서, 태그를 누르면 즉시 필터가 적용되고 눌린 태그는 채워진 스타일로 보이도록 변경(`onchange="this.form.submit()"` + `:has(:checked)` CSS, AND 다중 선택은 유지). 평점/날짜는 기존처럼 버튼으로 적용. (2) 사진 상세보기에서 태그별로 × 버튼을 추가해 개별 태그 삭제 가능(`database.unlink_tag()` 신설, `/photo/<id>/tag/remove` 라우트 추가, 삭제 전 확인창)
- **상세보기 레이아웃 원복**: 사진을 세로 중앙 정렬했던 걸(`align-items:center`) 사용자 요청으로 다시 상단 정렬(`start`)로 되돌림
- **개별 사진 수동 정정 요청 2건 — 아직 미반영**: `db9fb254`(블러 배경 위 부표 사진, CLIP은 사물 63.6%로 확신하지만 사용자는 자연이 맞다고 판단), `9a088720`(완전히 깜깜한 야경 반사 사진, CLIP은 사물/인물이 근소 우위 33%/32%로 자연 18.7%는 3위, 사용자는 자연이 맞다고 판단) — 둘 다 CLIP이 명확한 1등을 갖고 있어서 프롬프트를 더 건드리면 다른 사진에 부작용 튈 위험이 있다고 설명하고, "이 두 사진만 DB에서 직접 genre를 자연으로 고치자"고 제안까지 했으나 사용자가 다른 질문(CNN 활용 여부, 실내 태그 실종)으로 넘어가면서 실제 반영은 안 됨 — 다음 세션에서 이어서 처리 필요
- **`reclassify_photos.py`의 "변경" 카운터가 태그 변화를 안 잡는 것 발견**: genre가 안 바뀌어도 태그(예: `#실내`)는 바뀔 수 있는데, 콘솔에 찍히는 "📊 변경: N장"은 genre 변경만 셈 — 오늘 실내 화이트리스트 제거 때 실제로는 태그 24장이 바뀌었는데 "변경: 0장"으로 나와서 헷갈림. 고칠지 여부를 사용자에게 물었으나 아직 결정 안 됨(사소한 이슈라 급하지 않음)

### 다음 할 일

- **`db9fb254`/`9a088720` 수동 정정 미완료**: 사용자가 자연으로 고쳐달라고 요청했는데 아직 DB 반영 안 함 — 다음 세션에서 제일 먼저 처리
- **`a20242bb` 실내 태그 재검토**: 야시장 부스 사진, 프롬프트 v2로도 여전히 실내로 남음 — 오탐인지 정탐인지 불확실
- **`reclassify_photos.py` "변경" 카운터가 태그 변화 미반영**: genre만 비교해서 태그만 바뀐 경우 "0장 변경"으로 잘못 표시됨 — 고칠지 여부 미결정
- **daynight의 `04541da4` 회귀 케이스**: CLIP이 이 특정 사진에서 87.6%(CNN, 정답) → 65.1%(CLIP, 오답)로 역행함. 이 경로에 한해 CNN+CLIP 앙상블(둘 중 하나라도 강하게 확신하면 override)을 검토할 만함 — 오늘은 미착수
- **daynight EXIF의 "+00:00 아닌" 카메라 시계 오류 유형**: `5b9c3f20`(EXIF 23시인데 실제 대낮), 경계 근처가 아니라서 안전장치도 못 잡음 — 패턴이 더 모이면 재설계 검토
- **`도시`/`인물`의 벽/간판 클로즈업 등 실내/실외 오탐**: CLIP으로 대부분 해결됐으나 HAKATA 간판처럼 CLIP도 못 맞추는 잔여 케이스 소수 남음
- **장르 CLIP 전환으로 생긴 "#기타 정보 손실" 15건**: 5-way 단일 프롬프트라 확신도 60% 임계값을 못 넘는 경우가 늘어남(해변 풍선 사진 등, 위 완료 항목 참고). day/night·indoor처럼 클래스당 프롬프트를 2개 이상으로 늘려 확신도 분리를 개선하거나 임계값을 낮추는 방향 검토 가능 — 사용자가 "직접 보고 판단"하는 쪽을 택해서 오늘은 미착수, 브라우저로 직접 확인한 후 실제로 문제라고 판단되면 재검토
- **Late Fusion(메타 분류기) 아이디어 기록만, 착수 보류**: 사용자가 genre/indoor/daynight/OpenCV/EXIF/CLIP 출력을 전부 벡터로 합쳐 작은 메타 모델(Dense 16→8→2)로 최종 판단하는 방식을 제안. 개념은 유효하나 지금 규모(사진 82장, 확인된 오탐은 케이스별 1~3건)에는 학습 데이터가 턱없이 부족해서 재학습 CNN과 같은 과적합 함정에 빠지기 쉽고, 지금 진행 중인 "CNN 제거해서 단순화" 방향과도 역행함 — 사진이 수백~수천 장 쌓이고 반복되는 오탐 패턴이 보이면 재검토하기로 보류
- **"학습 버튼"(사용자가 잘못된 태그를 직접 고치면 그 데이터로 재학습) 기능**: 사용자 요청으로 논의했으나 보류. 실시간 학습은 불가능(학습 자체가 CPU로 수십 분 소요)하므로 "보정 데이터 축적 + 백그라운드 재학습 예약" 형태가 현실적. 오늘 재학습 역효과 사고를 고려하면, 재학습 후 실사진 검증을 통과해야만 서비스 파일로 승격시키는 자동 검증 게이트가 반드시 필요 — 설계만 하고 착수는 다음 세션으로 보류
- **실내/실외 데이터셋 후보 기록**(착수는 보류, 사용자 요청으로 기록만): [prithivMLmods/IndoorOutdoorNet](https://huggingface.co/prithivMLmods/IndoorOutdoorNet)(SigLIP2 파인튜닝 기성 모델, CLIP처럼 바로 갖다 쓸 수 있는 후보), [Google Street View(Kaggle, 1만 장)](https://www.kaggle.com/datasets/paulchambaz/google-street-view)(스트리트뷰 특성상 하늘 안 보이는 건물 근접샷이 많아 우리 실패 패턴과 정확히 맞음), [MIT Indoor Scenes(Kaggle)](https://www.kaggle.com/datasets/itsahmad/indoor-scenes-cvpr-2019)
- **텀블러/음료 애매 케이스(사물 vs 음식) 개선 방향 결정됨, 착수는 보류**: COCO처럼 생활 맥락 속에서 찍힌 물건 사진을 추가하는 방향(2026-07-08 항목 참고). 실행은 다음 세션으로 보류
- 음식/사물: confusion matrix로 정확한 오분류 패턴 확인 → 추가 데이터 보강 방향 결정
- 오늘 작업 git 커밋 필요(양이 많음 — daynight/indoor/genre/animal CLIP 교체, clip_utils.py 신설, README/DEVLOG 갱신, 결과물인 models/indoor_model.training.h5 잔재 없는지 확인 후)
- **환경 관련 메모**: 이 프로젝트의 실제 런타임(cv2/torch/transformers/tensorflow 전부 정상 설치)은 conda 환경 **`firstvenv`**(`/c/01Developkits/miniconda3/envs/firstvenv/python.exe`)이다. 오늘 시스템 기본 `python`(Python 3.14, `C:\01Developkits\Python\Python314`)으로 스크립트를 돌리다가 numpy/opencv 버전 불일치로 헤맨 적이 있음 — 그 과정에서 실수로 시스템 Python에 `opencv-python==4.10.0.84`를 설치해버렸음(프로젝트와 무관한 환경이라 실질적 피해는 없으나, 정리하려면 시스템 python에서 `pip uninstall opencv-python` 고려). 다음 세션에서도 `python`이 아니라 항상 `firstvenv`의 python 경로를 명시할 것

### 다음 세션 시작 프롬프트

다음 세션 시작할 때 이 내용을 그대로 붙여넣으면 됨:

```
tag_lens 이어서 하자. DEVLOG.md의 2026-07-09 항목 읽고 시작해줘.

오늘(2026-07-09) 완료: genre/indoor/daynight-폴백/animal 판별을 전부 CLIP
제로샷으로 교체, 카메라 시계 오류 감지, 얼굴 검출에 CLIP 크롭 2차 검증 추가
(화재경보기 오탐 + 배경 속 행인 문제 해결), 동물 태그에 mediapipe Object
Detector 기반 크기 게이트 추가(점만 한 새 오탐 제거), 실내/실외 판별을 모든
장르로 확대 + 프롬프트 개정(야간 매장 전면 오탐 수정), 태그 UI 2건(클릭 즉시
필터, 개별 태그 삭제). 실사용 테스트(서버 띄우고 82장 업로드, 사용자가 사진
하나하나 직접 확인)로 전부 실측 발견/검증함. git 커밋 완료(`1185a10`).
남은 것들 (우선순위 순):
1. **`db9fb254`/`9a088720` 수동 정정 미완료** — 사용자가 자연으로 고쳐달라고
   요청했는데 아직 DB 반영 안 함. 프롬프트를 더 건드리지 말고 이 두 사진만
   DB에서 직접 genre 수정하기로 방향은 정해짐, 실행만 하면 됨
2. `a20242bb`(야시장 부스) 실내 태그가 오탐인지 정탐인지 불확실 — 재검토 필요
3. `reclassify_photos.py`의 "변경" 카운터가 태그 변화를 안 잡음(genre만 비교)
   — 콘솔 메시지가 헷갈림, 고칠지 결정 안 됨
4. daynight `04541da4`(밤바다) 회귀 — CLIP이 이 사진 하나에서만 CNN보다 약함,
   CNN+CLIP 앙상블 검토 여지
5. daynight CNN(폴백용)이 원래 갖고 있던 EXIF "+00:00 아닌" 카메라 시계 오류
   유형(`5b9c3f20`, 경계 안전장치도 못 잡음) — 패턴이 더 모이면 재설계 검토
6. 장르 CLIP 전환으로 생긴 `#기타` 정보 손실 15건 — 사용자가 브라우저로 직접
   확인 후 재검토하기로 보류 중
7. `도시`/`인물`의 벽/간판 클로즈업 등 실내/실외 오탐 — HAKATA 간판처럼 CLIP도
   못 맞추는 잔여 케이스 소수 남음
8. "학습 버튼"/Late Fusion 아이디어 둘 다 논의만 하고 보류 — 지금 규모(오탐
   케이스별 1~3건)엔 데이터가 부족해서 시기상조
9. 실내/실외 데이터셋 후보, 텀블러/음료 애매 케이스 — 방향만 정해짐, 미착수
10. 환경 메모: 실제 런타임은 conda `firstvenv`, 시스템 기본 python(3.14)이
    아님 — 헷갈리지 말 것

먼저 현재 상태 확인하고 뭐부터 할지 정하자.
```

## 2026-07-08

### 완료

- **장르 CNN 5-class 확장**: 사물(1066장)·인물(2000장) 데이터를 새로 확보해 자연 2000 / 도시 938 / 음식 2000 / 사물 1066 / 인물 2000, train:val = 8:2로 데이터셋 구성
- **얼굴 검출기 mediapipe로 교체**: `opencv/face_detect.py`가 Haar Cascade → mediapipe(BlazeFace) 기반으로 교체됨
- **주간/야간 이진 분류기 추가**: `models/daynight_model.h5`, 검증 정확도 98.75%로 학습 완료
- **코드 공용화**: `ai/tagging.py`로 태그 생성 로직을 `app.py`/`scripts/reclassify_photos.py`가 공유하도록 정리, `database/database.py`에 `replace_tags()`/`update_classification()` 추가
- **파일 구조 정리**: 루트에 흩어져 있던 학습/유지보수 스크립트 6개(`prepare_genre_dataset.py`, `prepare_daynight_dataset.py`, `train_daynight_model.py`, `reclassify_photos.py`, `migrate_embeddings.py`, `_test_kaggle.py`)를 `scripts/`로 이동하고 경로 참조 수정
- **`.gitignore` 보강**: 실제 git 루트(`Python_project/`)의 `.gitignore`에 `__pycache__/`, `*.pyc`, `*.log`를 추가하고, 이미 커밋돼 있던 캐시 파일 11개를 추적 해제
- **`genre_model.h5` 2단계 fine-tuning 완료**
  - 1단계(base 동결, 15 epoch): val_accuracy 99.72%, val_loss 0.0125
  - 2단계(상위 30레이어 해동, `EarlyStopping patience=7`): epoch 14에서 조기 종료, 최고 성능이던 **epoch 7 가중치로 복원**(val_accuracy 99.72%, val_loss 0.0126) — fine-tuning으로 큰 개선은 없었지만 성능 저하 없이 안전하게 마무리됨
  - `train_genre_model.py`/`train_genre_model_phase2.py`도 학습 종료 후 `scripts/`로 이동 완료
- **얼굴 검출 파이프라인 복구**: 두 가지 문제가 겹쳐서 얼굴 검출이 완전히 죽어있던 걸 확인 후 수정
  - **mediapipe Solutions API 소실**: 설치된 mediapipe(0.10.35, Python 3.12 Windows 빌드)에 구버전 `mp.solutions.face_detection`이 아예 빠져있어서 (`tasks/`만 존재) `opencv/face_detect.py`가 임포트 시점에 죽고 있었음 → 신버전 **Tasks API**(`mediapipe.tasks.python.vision.FaceDetector`)로 재작성하고, `models/blaze_face_full_range.tflite` 모델 파일을 받아서 사용하도록 변경
  - **opencv-python 설치 자체가 손상됨**: `site-packages/cv2/`에 실제 모듈 파일(`__init__.py`, 컴파일된 `.pyd`)이 없이 DLL 하나만 남아있어 `cv2.imread` 등 모든 cv2 함수가 죽어있었음(얼굴 검출뿐 아니라 업로드 파이프라인 전체가 영향받는 문제였음) → `pip install --force-reinstall opencv-python==4.10.0.84`로 복구, 이 과정에서 딸려온 `numpy 2.5.1`(요구사항 `numpy<2.0`, tensorflow `numpy<2.1` 위반)도 `numpy==1.26.4`로 다시 고정
  - 얼굴 있는 사진/없는 사진 양쪽으로 검출 결과 확인 완료
- **CNN 분류 확률을 DB에 저장**: 기존엔 업로드 직후 `session`에만 임시로 담아뒀다가 result 페이지 한 번 렌더링하고 날아갔음 → `photo` 테이블에 `genre_probs`/`daynight_probs` 컬럼 추가, 업로드·재분류 시 DB에 저장, `/result/<id>`가 언제 봐도(갤러리에서 나중에 상세보기 들어가도) 동일한 확률을 보여주도록 변경
- **유사 사진 검색 임베딩 교체**: 기존엔 5-class 장르 분류기(`genre_model.h5`)의 분류 직전 레이어(128차원)를 유사도 임베딩으로 재사용했는데, 이 레이어는 fine-tuning 과정에서 "5개 장르 구분"에만 최적화되면서 같은 장르 안의 시각적 차이를 뭉개버려 무관한 사진끼리도 코사인 유사도 0.9+가 나오는 문제가 있었음(실측 확인됨) → 장르로 fine-tuning되지 않은 순수 ImageNet 사전학습 MobileNetV2(1280차원, `ai/genre_predict.py`의 `_get_feature_extractor()`)로 교체, 기존 19장 임베딩도 재계산 완료
- **흑백 사진 주야간 판별 스킵**: 흑백은 색온도/밝기 신호가 왜곡되므로 `opencv/feature_analyzer.py`의 `is_bw_image()`로 판별해 `predict_daynight()`가 `(None, None)`을 반환하도록 변경 — `ai/tagging.py`가 None이면 주야간 태그를 안 붙임
- **얼굴 검출 오탐 수정**: mediapipe full-range 모델이 노을 진 구름 텍스처를 얼굴로 착각(신뢰도 0.53~0.54)해서 사람 없는 풍경 사진이 `인물`로 잘못 분류되는 사례 발견 → `min_detection_confidence`를 0.5→0.6으로 상향(실제 얼굴은 0.64~0.91로 훨씬 높아 영향 없음 확인)
- **CNN 저확신 예측 → `기타` 분류**: 애매한 사진(예: 인물 53.6% vs 음식 40.8%)이 5개 장르 중 하나로 억지로 배정되던 문제 → `ai/genre_predict.py`에 `LOW_CONFIDENCE_THRESHOLD=0.6` 추가, 최고 확률이 이 미만이면 `기타`로 분류하고 `#기타` 태그 부여. 확신도 높은 예측(98~100%)은 영향 없음 확인
- **`풍경` → `자연` 태그 명칭 변경**: 코드 전반(`CLASS_NAMES`, 태그 매핑, `dataset/{train,val}/풍경` 폴더 등) 일괄 변경
- **`#흑백` 태그 추가**: `is_bw`는 DB 컬럼으로만 있고 태그화가 안 돼있었음 → `ai/tagging.py`에 반영
- **실내/실외 CNN 추가 착수**: HuggingFace `prithivMLmods/IndoorOutdoorNet-20K`(2만 장, Apache-2.0, 도메인 다양)를 클래스당 2500장으로 축소해 로컬 학습 진행 중. `자연`/`도시`/`인물` 장르에만 적용(`사물`/`음식`은 배경 맥락 없어 제외), `#실내`만 태그(실외는 자연/도시 태그가 이미 커버해서 생략)
- **`#따뜻한`/`#차가운` 색온도 태그 추가**: `color_tone`도 `#흑백`처럼 DB에만 있고 태그화가 안 돼있었음 → 흑백 사진은 색 정보가 왜곡되므로 색온도 태그를 붙이지 않도록 예외 처리
- **README를 README.md(개요)와 DEVLOG.md(작업 로그)로 분리**
- **장르 5-class + 주야간 CNN 재학습 (Colab GPU) 완료**: `사물` 카테고리 18개 확장(Caltech-101 캐시 재활용, 1066→2136장), 주야간 데이터셋에 5-class 이미지 밝기 기반 pseudo-label 추가(400→약 2160장)로 도메인 다양성 확보 후 Colab에서 재학습(`scripts/colab_train.ipynb`). 완료된 `genre_model.h5`/`daynight_model.h5`를 로컬 `models/`에 반영하고 검증(5-class/2-class 출력 shape 확인, 실제 사진으로 예측 확인)
  - `ImageDataGenerator`가 CPU 싱글스레드로 데이터를 로딩해 GPU 대비 병목이 있다는 걸 확인했음(에폭당 로컬과 큰 차이 없는 속도) — 다음에 다시 쓸 일이 있으면 `tf.data` 파이프라인이나 `workers` 병렬화 고려
- **실내/실외 CNN 로컬 학습 완료**: `scripts/train_indoor_model.py`, 클래스당 2500장, val_accuracy 100%(loss 0.0041). 실사용 사진으로 검증한 결과 대체로 잘 맞으나, **하늘이 안 보이는 실외 사진(건물 파사드 클로즈업, 저녁 시간대 따뜻한 실내조명이 창문으로 비치는 구도)을 실내로 오분류하는 경향 확인**(99.9% 확신도로 틀림 — 확신도 임계값으로는 못 거름). 실사용 가능한 수준이나 이 약점은 남아있음
- **`python scripts/reclassify_photos.py`로 기존 35장 재분류**: 새 genre/daynight/indoor 모델 + `자연` 태그명 + `#흑백`/`#따뜻한`/`#차가운`/`#실내` 태그 전부 반영, 5장(14.3%) 장르 변경 확인
- **텀블러/음료 사진이 `음식`으로 분류되는 사례 확인**: 재학습 후에도 동일 — 근본 원인은 데이터 부족이 아니라 **장르 정의 자체의 애매함**(내용물 든 음료잔 클로즈업이 사물 학습 데이터의 "빈 컵 스튜디오샷"보다 음식 학습 데이터의 "카페 음료 클로즈업"과 시각적으로 더 유사함) — 보류, 아래 "다음 할 일" 참고
- **업로드 응답 속도 개선**: `predict_genre()`/`predict_daynight()`/`predict_indoor()`가 매 업로드마다 `.h5` 모델을 디스크에서 새로 로드하고 있었음(요청당 1~3초씩 낭비) → `extract_features()`에 이미 적용했던 것과 같은 전역 캐시 패턴(`_MODEL_CACHE`)을 세 함수에 모두 적용. 실측: 최초 1회(모델 로드 포함) 8.58초 → 캐시 재사용 시 0.22초(약 39배). 서버 켜고 첫 사진만 느리고 이후로는 거의 즉시 처리됨
- **주간/야간 pseudo-label 오염 발견 및 수정**: 아침에 도메인 다양성 확보용으로 5-class 이미지에 밝기 기반 pseudo-label을 붙여 `dataset_daynight`를 4배 이상 늘렸는데(400→2160장), **"화면 전체 평균 밝기가 낮으면 야간"이라는 기준이 틀렸음이 확인됨** — 그림자 짙은 대낮 숲, 어두운 옷의 인물사진처럼 "낮이지만 어두운 톤"인 사진이 대량으로 `야간`으로 잘못 라벨링돼 모델이 오염됨(pseudo-label이 실라벨보다 4배 많아서 지배적이었음). 상위 퍼센타일 밝기로 바꿔봐도 고대비 장면(일부만 밝음)엔 여전히 안 먹힘 → **pseudo-label 전량 제거, 원 데이터(400장, CCTV 도로 도메인)만으로 재학습**(val_accuracy 97.53%, 이전의 의심스러운 100%보다 현실적)
- **주간/야간 도메인 다양성 재확보 (DNIM)**: 위 재학습만으로는 도메인이 다시 좁아져 자연/사물/음식 사진에 약함 → Kaggle `stevemark/daynight-dataset`(DNIM, Archive of Many Outdoor Scenes 기반, **17개의 서로 다른 웹캠 위치**)를 받아, **밝기 추정이 아니라 파일명에 박힌 실제 촬영 시각**(`20151101_072507.jpg` 등)으로 확실한 낮(08~16시)/밤(21~04시) 시간대만 골라 추가(주간 682/169, 야간 626/154). 재학습 진행 중(백그라운드)
- **실내/실외 오분류 패턴 발견**: 반사 유리 건물, 화보 촬영처럼 **반사면+블러 배경**이 있는 사진을 실내로 오판(확신도 90%대 이상)하는 경향 확인. 원인은 라벨 오염이 아니라 학습 데이터(HuggingFace IndoorOutdoorNet-20K)가 일반 캐주얼 사진 위주라 전문/예술 사진 스타일이 부족했던 것 → 기존 로컬 `도시`(Intel buildings/street, 751장, 전부 실외) 재활용 + **Places365**(MIT, 365개 장면 카테고리, 공식 indoor/outdoor 매핑 제공)에서 스트리밍으로 1000장씩(실내/실외 각각) 다양한 스타일 추가(train 2800/3576, val 700/862). 재학습 진행 중(백그라운드)
- **주간/야간 판별에 EXIF 촬영시각 우선 사용**: CNN으로 픽셀에서 낮/밤을 추정하는 것보다, 사진에 이미 있는 EXIF `DateTimeOriginal`이 훨씬 신뢰도 높은 실측 신호라는 걸 뒤늦게 깨달음 → `app.py`/`scripts/reclassify_photos.py`에 `extract_exif_hour()` 추가, EXIF 시각이 있으면 `daynight_from_hour()`(06~17시=주간, 그 외=야간)로 확정 판별하고 **EXIF가 없을 때만 CNN(`predict_daynight`)으로 폴백**. 실측: 업로드 테스트 사진 14장 전부 EXIF 있었고, CNN이 계속 틀렸던 대낮 숲 사진도 EXIF로는 정확히 "주간" 판정됨. 흑백 사진에도 적용 가능(색 정보 불필요)
- **파일 구조 재정리**:
  - `_raw_sorted/`(2.6GB, `prepare_genre_dataset.py`의 임시 중간 산출물, 스크립트 자체가 "삭제해도 됨"이라 명시) 삭제
  - 루트/`scripts/`의 `__pycache__` 정리
  - 완료된 학습 로그(`phase2_train.log`, `indoor_train.log`, `daynight_retrain.log`, `daynight_retrain2.log`, `places365_expand.log`)를 `logs/`로 이동 — 진행 중인 로그(`flask_run.log`, `indoor_retrain.log`)는 프로세스가 물고 있어 그대로 둠
  - 폐기된 `scripts/expand_daynight_dataset.py`(pseudo-label 방식) 삭제
  - **`dataset_daynight/`에 pseudo-label 잔재 16장 추가 발견 및 제거**: 이전 정리 때 `pseudo_*.jpg`만 지웠는데, Caltech-101 소스 이미지 중 `.png`/`.jpeg` 확장자인 pseudo 파일 16개가 안 지워지고 남아있었음 → 두 번째 daynight 재학습(DNIM 버전)에도 이 16장이 섞여 들어갔었음(전체의 약 1.2%, 영향은 작지만) → 완전 제거 후 최종 daynight 데이터셋은 100% 실라벨(주간 673/168, 야간 620/154)
  - 장르 스크립트 이름이 daynight/indoor랑 안 맞았음(`prepare_dataset.py`/`train_model.py`/`train_model_phase2.py`만 "genre"가 이름에 안 담김) → `prepare_genre_dataset.py`/`train_genre_model.py`/`train_genre_model_phase2.py`로 이름 통일, import·문서·`colab_train.ipynb` 참조 전부 수정
  - **`opencv/face_detect.py` → `ai/face_detect.py` 이동**: 이 파일은 이름/폴더 위치와 달리 실제로는 OpenCV가 아니라 mediapipe(구글 사전학습 모델)를 씀 — `opencv/`에는 진짜 OpenCV만 쓰는 코드(전처리, 특징 분석)만 남기고, "판단 로직"은 전부 `ai/`로 모음. 단 mediapipe는 우리가 학습시킨 게 아니라 외부 사전학습 모델이라, 같은 `ai/`여도 genre/daynight/indoor_predict.py(직접 학습)와는 다르다는 걸 모듈 docstring에 명시
  - **`ai/genre_predict.py`에서 유사도 검색용 특징 추출 로직을 `ai/embedding.py`로 분리**: 한 파일 안에 "직접 학습한 장르 CNN"(`predict_genre`)과 "순수 사전학습 MobileNetV2 그대로 씀"(`extract_features`)이 섞여 있었음 — 위 face_detect.py 구분과 같은 원칙으로, 직접 학습 여부에 따라 파일 자체를 분리. `app.py`/`scripts/migrate_embeddings.py`의 import 갱신
    - 이 과정에서 Flask 자동 재시작이 파일 수정 중간 타이밍에 걸려(구 `app.py` import + 신 `genre_predict.py`) `ImportError`로 서버가 죽은 적 있음 → import 수정 완료 후 재시작해서 복구
- **`#야경` 태그 조건에 `자연` 장르 추가**: 기존엔 `도시`+`야간`일 때만 붙었는데, `자연`(별이 보이는 산/바다 야경 등)도 야경으로 쳐야 한다는 피드백 반영 — `genre in ("도시", "자연") and daynight == "야간"`으로 변경
- **흑백 사진 주야간 스킵 규칙이 EXIF 경로에서 빠져있던 것 수정**: 원래 "흑백이면 주야간 판별 안 함" 규칙은 `predict_daynight()`(CNN) 안에만 있었는데, 오늘 추가한 EXIF 우선 판별(`daynight_from_hour()`)은 이 체크를 안 거치고 바로 태그를 붙이고 있었음 → `app.py`/`scripts/reclassify_photos.py`에서 EXIF 확인보다 먼저 `is_bw_image()`(또는 `features["is_bw"]`)를 체크해서, 흑백이면 EXIF 유무와 무관하게 무조건 `(None, None)`으로 스킵하도록 수정
- **EXIF `DateTimeOriginal`이 최상위 IFD가 아니라 Exif SubIFD에 있는 걸 놓친 버그 수정**: `image.getexif()`는 IFD0만 반환하는데, 실제 촬영시각(`DateTimeOriginal`, 36867)은 중첩된 Exif SubIFD(태그 34665가 가리킴)에 들어있음 → 우리 코드가 이걸 못 찾고 IFD0의 `DateTime`(306)으로 폴백했는데, **이 306번 태그는 Lightroom/포토샵 같은 편집 프로그램이 "내보내기 시각"으로 덮어써버리는 경우가 많아** 실제 촬영 시각과 전혀 다른 값이 됨(확인된 사례: 실제 촬영 11:26, Lightroom 내보내기 19:29 — 7시간 넘게 차이). `_get_exif_datetime_str()` 헬퍼로 통합해서 SubIFD의 `DateTimeOriginal`/`DateTimeDigitized`를 먼저 보고, 없을 때만 IFD0의 `DateTime`을 최후 폴백으로 사용하도록 수정. `extract_photo_date()`(업로드 날짜 표시)도 같은 버그였어서 같이 고쳐짐 — 편집된 사진들의 업로드 날짜도 부정확했을 것
- **`ai/genre_predict.py`가 흑백 사진에서도 정상 작동함을 검증**: 컬러→흑백 변환 테스트에서 5개 장르 전부 100% 정확 — 특정 사진(신발 매장 진열대, 91.4% 확신도로 `음식` 오분류)의 원인은 흑백 처리 버그가 아니라, 매장 진열대처럼 5개 장르 어디에도 안 맞는 구도라 CNN이 억지로 하나 고른 것(텀블러 사례와 동일 패턴) — 코드 버그 아님을 확인, 별도 조치 없이 알려진 한계로 기록
- **`scripts/` 하위 폴더 재정리**: 14개 스크립트가 한 폴더에 평평하게 쌓여있어 헷갈린다는 피드백 → `prepare/`(데이터셋 원본 구성) / `expand/`(기존 데이터셋 보강) / `train/`(CNN 학습) / `maintenance/`(DB에 이미 있는 사진에 결과 반영) 4개 하위 폴더로 분리. 한 단계 깊어진 만큼 모든 스크립트의 `BASE_DIR = Path(__file__).resolve().parent.parent` → `.parent.parent.parent`로 수정, `IO_places365.txt`도 이를 참조하는 스크립트와 같은 폴더(`expand/`)로 이동, 전부 import/경로 재검증 완료
- **실내/실외 "하늘 안 보이는 실외 사진" 오분류 — Places365/도시 보강 후에도 미해결, 여기서 중단**: 재학습 후 재검증한 결과 확신도는 낮아짐(TSINGTAO 99.9% → PIMFY 매장 외벽 74.0%)이나 여전히 실내로 오분류. Places365와 우리 도시 데이터 모두 "공간 전체가 보이는" 사진 위주라 "벽/간판에 딱 붙은 클로즈업" 구도 자체가 부족했던 것으로 보임 — 추가 데이터 보강 없이 알려진 한계로 남기고 오늘은 여기서 종료
- **동물 세부 태그(`#강아지`/`#고양이`/`#말`/`#동물`) 추가**: 새로 학습하지 않고 이미 쓰던 ImageNet 사전학습 MobileNetV2를 `include_top=True`(1000-class 그대로)로 한 번 더 로드해서 판별. `imagenet_class_index.json`을 직접 훑어서 인덱스 0~397번이 ImageNet-1k의 동물 클래스 전체(398번 abacus부터 사물/장면)임을 확인 후, 그 안에서 강아지(151~268)·고양이(281~285)만 넓은 범위라 세분화하고, 말은 별도 "horse" 클래스가 없어 밤색 말을 뜻하는 "sorrel"(339번)을 사실상 말 클래스로 사용(얼룩말 340 zebra는 별개 동물 취급). 나머지 동물 범위는 전부 `#동물`로 통합. `ai/animal_predict.py` 신규 작성(embedding.py와 같은 "순수 사전학습, 직접 학습 안 함" 패턴), `ai/tagging.py`/`app.py`/`scripts/maintenance/reclassify_photos.py`에 연결. top-1 확신도 0.3 미만이면 태그 없음. 인덱스 매핑 로직은 모델 출력을 mock해서 강아지/고양이/말/동물/비동물/저확신 6개 케이스 전부 검증 완료, 실제 파이프라인은 건물 사진(비동물)으로 정상적으로 `None` 반환하는 것까지 확인(동물 사진은 현재 dataset에 없어 직접 검증은 못함)

### 알려진 이슈

- **`tag_lens.db`가 git에 추적 중**: 실제 사진이 쌓이면 DB가 계속 바뀔 텐데, 계속 커밋할지 `.gitignore`에 추가할지 아직 결정 안 됨
- **실내/실외 CNN이 하늘 안 보이는 실외 사진(건물 벽/간판 클로즈업)을 실내로 오분류**: Places365 + 도시 데이터로 한 차례 보강했으나(위 참고) 여전히 남아있음. 확신도는 낮아졌지만(99.9%→74%) 단순 임계값으로 거르기엔 애매함 — 재시도한다면 "벽/간판에 딱 붙은 클로즈업" 구도의 실외 사진을 콕 집어 보강해야 함
- **텀블러/음료 등 "내용물 든 용기" 사진이 사물 대신 음식으로 분류됨**: 위 완료 항목 참고 — 장르 정의 자체가 애매한 경계 케이스, 데이터 추가 시 반대급부(진짜 음식 사진이 사물로 잘못 갈 위험) 있음
- **CNN 입력 정규화가 MobileNetV2 사전학습 방식과 불일치**: `train_genre_model.py`/`train_daynight_model.py`/`train_indoor_model.py`와 `opencv/preprocess.py`의 `prepare_for_cnn()`이 `rescale=1./255`(0~1 범위)를 쓰는데, `weights="imagenet"` MobileNetV2는 원래 `mobilenet_v2.preprocess_input`(-1~1 범위)로 학습됨. 오늘 새로 만든 `extract_features()`(유사도 검색용)는 이걸 맞게 썼는데 정작 분류기 학습/추론 쪽은 안 맞음. 학습·추론이 서로 일관돼 있어서 당장 오작동하는 건 아니고(치명적이지 않음), 전이학습 효과를 100% 못 뽑아내고 있을 뿐 — 재학습 없이는 못 고치는 문제라 **다음에 어차피 재학습할 일이 생기면 그때 같이 반영**하기로 보류

- **`#흑백` 판별 로직을 "전체 평균" 대신 "색 픽셀 비율"로 교체**: 기존 `is_bw_image()`는 이미지 전체의 채널 표준편차를 평균내서 8.0과 비교했는데, 이러면 국소적으로 진짜 색이 있어도(예: 금속에 반사된 색이 전체 픽셀의 4.7%) 나머지 무채색 영역에 희석돼 흑백으로 오판하는 사례 발견(기계 다이얼 매크로 사진, 반사광 색상 BGR=[2,83,134] 등 뚜렷한 색 확인됨). 반대로 "색 픽셀이 조금이라도 있으면 컬러"로 하면 JPEG 압축 노이즈만으로 진짜 흑백 사진까지 컬러로 오판하게 됨 — 실측(컬러 30장 vs 그레이스케일→JPEG 재인코딩 30장)으로 진짜 흑백은 색 픽셀 비율 0%, 실제 컬러는 대부분 60~94%로 명확히 갈리는 걸 확인하고, 픽셀별 채널표준편차 > 8인 픽셀이 전체의 2% 이상이면 컬러로 판정하는 절충 로직으로 교체. `reclassify_photos.py`로 기존 26장 재반영, 흑백→컬러로 뒤집힌 사진들 확인됨
- **주간/야간 3중 다수결(EXIF+CNN+밝기 휴리스틱) 시도했다가 되돌림**: 카메라 시계 오류로 EXIF가 틀린 사례(화보 사진, 실제 대낮인데 EXIF는 00:39로 기록됨 — OffsetTimeOriginal +00:00 vs OffsetTime +08:00 불일치로 시간대 오설정 추정)를 발견하고 다수결로 보완하려 시도했으나, **CNN과 밝기 휴리스틱이 둘 다 픽셀 밝기 기반이라 독립적이지 않다는 게 실측으로 확인됨**(그림자 짙은 대낮 숲 사진 250장 중 118장(47%)에서 CNN이 야간으로 오판, 그중 다수는 휴리스틱도 동일하게 오판 — 이 경우 다수결이 2:1로 정확한 EXIF를 뒤집어버림). 카메라 시계 오류는 극소수 사례인 반면 이 역효과가 더 크다고 판단해 원래 EXIF 우선 방식으로 복구(`ai/daynight_predict.py`의 `resolve_daynight()`로 app.py/reclassify_photos.py 공유 로직은 통합해둠 — 흑백 스킵 체크가 EXIF 분기에서 빠질 뻔한 것도 이 통합 과정에서 재확인). 재설계는 보류(아래 "다음 할 일" 참고)
- **daynight CNN이 숲/자연 사진에 광범위하게 약하다는 것 신규 발견**: 위 다수결 검증 과정에서 발견 — `dataset/train/자연`의 숲 사진 250장 중 47%가 CNN에 의해 야간으로 오판됨(23장은 85% 이상 확신도로 자신있게 틀림, 최대 99.5%). 기존엔 "가끔 틀리는 사례 하나" 정도로만 알고 있었는데 실제로는 훨씬 광범위한 약점이었음 — EXIF 우선 로직 덕분에 EXIF 있는 사진은 영향 없지만, EXIF 없는 숲/나무 사진은 여전히 이 문제에 노출됨
- **`database.find_similar()`의 "임베딩 없으면 해시로 폴백" 로직이 실제로는 죽어있던 버그 발견 및 정리**: `app.py`가 `photo.get("embedding") or photo.get("hash")`로 해시를 유사도 검색에 넘기고 있었는데, `find_similar()`는 해시 문자열(64자리 0/1)을 JSON 정수로 파싱해버려서(예: `json.loads("110100...")`가 거대한 정수로 파싱됨) `np.array(..., dtype=float32)`가 오버플로로 `inf`가 되는 등 사실상 의도대로 동작한 적이 없었음. 지금은 모든 사진이 업로드 시점에 임베딩을 항상 갖게 돼서(오늘 이전 세션에 이미 기존 사진 전부 마이그레이션 완료) 이 폴백 경로 자체가 도달 불가능한 죽은 코드였음 — 해시 폴백 제거하고 `find_similar()`는 임베딩만 다루도록 정리, 0벡터 폴백 차원도 실제 임베딩 차원(1280)에 맞게 수정(기존엔 1024로 틀려있었음)
- **`update_classification()`이 is_bw/color_tone/brightness/saturation을 안 갱신하던 것 발견 및 수정**: `reclassify_photos.py`가 새 `is_bw_image()` 로직으로 태그는 새로 계산해서 반영하면서도, `photo` 테이블의 `is_bw`/`color_tone`/`brightness`/`saturation` 원본 컬럼은 그대로 둬서 태그와 DB 컬럼이 어긋나는 상태가 됨(지금은 이 컬럼들을 화면에 직접 보여주는 곳이 없어서 눈에 띄는 버그는 아니었음). `update_classification()`에 선택 인자로 추가해서 재분류 시 같이 갱신되도록 수정
- **전체 주석 검토**: `scripts/` 하위 13개 파일은 리팩터링 이후에도 stale 참조 없이 깨끗한 상태 확인. `reclassify_photos.py`의 모듈 docstring이 실제 갱신 항목(indoor/animal 등)을 다 담지 못하고 있던 것 발견해 수정
- **실내/실외 판별에도 저확신 스킵 로직 추가**: `predict_indoor()`가 장르 CNN(`LOW_CONFIDENCE_THRESHOLD`)과 달리 확신도 체크 없이 51% vs 49%인 애매한 경우도 무조건 실내/실외를 확정하고 있던 것 발견 → 동일한 임계값(0.6) 패턴 적용, 미만이면 `indoor=None`으로 태그 자체를 건너뜀. 검증 데이터셋(120장)에서 회귀 없음 확인(여전히 100% 정확도, 스킵 0건 — 학습 데이터와 비슷한 사진은 항상 고확신으로 나온다는 뜻). 단, 이 로직은 "애매해서 낮은 확신"만 걸러주고, 반사 유리 건물처럼 "확신 있게 틀린" 케이스(98%)는 못 거름 — 별개 문제로 남아있음
- **장르 CNN이 동물 클로즈업을 사람으로 오분류하는 문제 발견 및 보정**: 고양이 얼굴 클로즈업 사진이 96.6% 확신도로 "인물"로 분류돼 `#인물 #고양이`라는 모순된 태그 조합이 나온 사례 발견. 원인은 "인물" 학습 데이터가 전부 사람 얼굴 클로즈업(정면 응시·큰 눈·얼굴 중심 구도)이라, 같은 구도의 동물도 사람으로 일반화해버린 것(negative 예시 부재) — 사물/음식 경계 모호함(텀블러), 실내/실외(반사 유리) 문제와 같은 계열의 학습 데이터 다양성 부족. `ai/genre_predict.py`에 `resolve_genre_with_animal()` 추가: mediapipe가 사람 얼굴을 못 찾았는데(face_count==0) `animal_predict`가 동물을 감지했다면 "인물" 판정을 "기타"로 보정. "동물"을 별도 장르로 만들지 않은 이유는 genre_probs가 5-class CNN 그대로라 근거 없는 6번째 장르를 만들면 확률 표시와 모순되기 때문(`#고양이` 태그가 이미 "무엇인지" 알려주므로 `#기타 #고양이` 조합으로 충분). `app.py`/`reclassify_photos.py` 모두 animal 예측 시점을 genre 확정 직후로 옮겨서, 보정된 genre가 이후의 실내/실외 판별에도 일관되게 반영되도록 순서 조정. 실제 인물 사진(val 80장)으로 회귀 테스트해서 오탐(진짜 인물이 잘못 기타로 바뀌는 경우) 0건 확인 후 기존 DB에도 반영(고양이 사진 1장이 인물→기타로 정정됨)

### 다음 할 일

- **주간/야간 판별 재설계 필요**: EXIF 우선 방식은 카메라 시계 오류에 취약하고, CNN/휴리스틱은 그림자 짙은 대낮 사진(특히 숲)에 취약함 — 서로 다른 실패 모드라 단순 다수결로는 안 풀림. 카메라 시계 오류를 직접 감지하는 방향(예: `OffsetTimeOriginal` vs `OffsetTime` 불일치 체크)이 밝기 기반 휴리스틱보다 더 독립적인 신호일 수 있어 다음에 시도해볼 만함
- **daynight CNN의 숲/자연 사진 오분류(47%) 원인 파악 및 재학습 필요**: EXIF 없는 숲 사진에 실질적으로 영향. 학습 데이터에 나무 그늘/캐노피 도메인이 부족했을 가능성
- **동물 태그를 실제 강아지/고양이/말 사진으로 검증 필요했던 항목 — 완료**: 재분류 중 실제 업로드 사진에서 `#고양이` 태그가 정상적으로 붙는 것 확인됨
- 실내/실외: "하늘 안 보이는 실외(벽/간판 클로즈업)" 오분류 — 한 차례 보강 시도했으나 미해결(위 참고), 추가로 파고들지 이 정도 한계로 둘지 다음에 결정
- **텀블러/음료 애매 케이스(사물 vs 음식) 개선 방향 결정됨, 착수는 보류**: 원인은 `사물` 학습 데이터(Caltech-101)가 전부 흰 배경 스튜디오 제품샷이라 스타일/맥락 다양성이 부족한 것 — 카테고리를 더 늘리는 건 비효율적(장르 분류는 세분류/객체 인식과 달리 물건 종류 자체를 구분할 필요가 없음)이고, 대신 **COCO(Common Objects in Context)처럼 생활 맥락 속에서 찍힌 물건 사진**을 추가해 "이런 촬영 스타일/구도도 사물이다"를 가르치는 방향이 맞음. 실행은 다음 세션으로 보류
- 음식/사물: 재학습 후에도 여전히 안 좋으면 confusion matrix로 정확한 오분류 패턴 확인 → 추가 데이터 보강 방향 결정
- 오늘 작업 전체 git 커밋 (아직 미완료 — 43개 이상 변경 사항 누적)
- **보류**: 계층형 태그 체계 확장(예: 장르/피사체/환경/시각요소로 나눠 건물·간판·타이포그래피·미니멀·기하·색상 등 세분화). 난이도가 태그마다 완전히 다름:
  - 색상(예: 빨간색): 쉬움 — 이미 있는 색온도/채도 분석(`opencv/feature_analyzer.py`)에서 지배적 색상 추출로 확장 가능, CNN 불필요
  - 텍스트 유무/타이포그래피: 중간 — OCR/텍스트 검출기(EAST, Tesseract 등) 필요, 지금까지 쓴 분류 CNN과는 다른 종류의 모델
  - 건물/간판: 어려움 — "사진 전체 장르"를 맞추는 지금의 분류(classification)가 아니라 사진 안에서 위치까지 찾는 객체 탐지(object detection, YOLO류)가 필요. 지금 프로젝트에 없는 종류의 모델+데이터
  - 미니멀/기하: 가장 어려움 — 객관적 정답이 없는 미적 판단이라 특징 기반 추정은 신뢰도가 낮고, 제대로 하려면 사람이 라벨링한 데이터로 별도 학습 필요
  - 착수한다면 색상 → 텍스트 유무 순으로 먼저, 객체 탐지·미적 판단류는 훨씬 큰 별도 작업으로 나중에

### 다음 세션 시작 프롬프트

다음 세션 시작할 때 이 내용을 그대로 붙여넣으면 됨:

```
tag_lens 이어서 하자. DEVLOG.md의 2026-07-08 항목 읽고 시작해줘.

오늘 남은 것들:
1. 주간/야간 판별 재설계 — EXIF 우선 방식은 카메라 시계 오류에 취약하고
   CNN/밝기휴리스틱은 그림자 짙은 대낮 숲 사진에 취약해서 단순 다수결로는
   안 풀림. OffsetTimeOriginal vs OffsetTime EXIF 태그 불일치로 카메라 시계
   오류를 직접 감지하는 방향 검토해보자.
2. daynight CNN이 숲/자연 사진 47%를 야간으로 오분류하는 문제 원인 파악
   (재학습 데이터에 나무 그늘/캐노피 도메인 보강 필요할 수 있음)
3. 실내/실외 "하늘 안 보이는 실외(벽/간판 클로즈업, 반사 유리 건물)" 오분류
   — 계속 파고들지 알려진 한계로 남길지 결정
4. 텀블러/음료 사물 vs 음식 애매 케이스 — COCO 스타일 데이터 추가하는
   방향으로 결정은 됐는데 착수는 안 함
5. git 커밋 아직 안 함 — 오늘 변경사항 전부(44개 파일) 미커밋 상태로 남아있음,
   커밋 메시지/범위 상의 필요

먼저 현재 상태 확인하고 뭐부터 할지 정하자.
```

### 환경 메모

- 실제 패키지(tensorflow/opencv/kagglehub/mediapipe/datasets)는 conda 환경 **`firstvenv`**(Python 3.12)에 설치돼 있음. 프로젝트 루트의 `.venv`는 비어있는 미사용 환경이므로 혼동 주의
- 로컬 GPU 없음 — CPU 학습은 MobileNetV2 기준 에폭당 100~250초(데이터 규모에 비례). 규모가 크면 Google Colab(GPU T4)이 훨씬 빠르지만, `ImageDataGenerator` 기반 파이프라인은 데이터 로딩이 CPU 싱글스레드라 GPU 이점이 상당 부분 상쇄될 수 있음(확인됨) — 다음에 다시 쓸 일이 있으면 `workers`/`use_multiprocessing` 병렬화나 `tf.data` 파이프라인으로 교체 고려
